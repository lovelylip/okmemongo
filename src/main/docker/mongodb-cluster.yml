version: '2'
services:
  okme-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/okme-mongodb-config --bind_ip 0.0.0.0
  okme-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  okme-mongodb-config:
    image: mongo:4.2.7
    container_name: okme-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
